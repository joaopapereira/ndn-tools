language: cpp

compiler:
  - gcc

before_install:
  - if [ $TRAVIS_OS_NAME == linux ]; then export NODE_LABELS=´cat /etc/lsb-release | grep DISTRIB_DESCRIPTION | cut -d"=" -f2 | sed 's/ /-/g'´; fi
  - echo $NODE_LABELS
  - .jenkins.d/00-deps.sh
  - .jenkins.d/10-ndn-cxx.sh
install: true

script: .jenkins.d/20-build.sh

branches:
  only:
    - master
os:
  - linux
